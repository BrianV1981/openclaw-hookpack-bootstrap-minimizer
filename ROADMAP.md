# Roadmap

## Next
- Make per-agent "wanted lists" configurable via `openclaw.json` so non-coders can customize what each agent loads without editing `handler.ts`.
  - Examples:
    - Agent A loads nothing
    - Agent B loads only `SOUL.md`
    - Agent C loads default but omits `TOOLS.md`
